# Render Blueprint — one-click free deploy of Shorts Factory
# Docs: https://render.com/docs/blueprint-spec
services:
  - type: web
    name: shorts-factory
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npx drizzle-kit push --force && npm run start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_VERSION
        value: 20
      # If using Neon, paste your Neon pooled URL here in the Render dashboard.
      # If left as-is, it connects to the built-in free Render database below.
      - key: DATABASE_URL
        fromDatabase:
          name: shorts-factory-db
          property: connectionString
      - key: DATABASE_URL_UNPOOLED
        sync: false
      - key: AUTH_SECRET
        generateValue: true
      - key: YOUTUBE_API_KEY
        sync: false
      - key: APP_URL
        sync: false
      - key: YT_CLIENT_ID
        sync: false
      - key: YT_CLIENT_SECRET
        sync: false
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: OPENAI_API_KEY
        sync: false
      - key: GITHUB_TOKEN
        sync: false
      - key: GITHUB_STATE_REPO
        value: lasttopper/shorts-factory
      - key: CRON_SECRET
        sync: false

databases:
  - name: shorts-factory-db
    plan: free
    databaseName: shorts_factory
    user: shorts_factory
    postgresMajorVersion: "16"
